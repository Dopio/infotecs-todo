import React, {useReducer} from 'react'

let reducer = (state, action) => {
    switch (action.type) {
        case value:
            return state

        default:
            return state;
    };
};


const [data, dispatch] = useReducer(reducer, )